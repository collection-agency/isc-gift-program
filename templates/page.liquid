<section>
  <div class="flex flex-wrap -mx-4 px-4 justify-between">
    <div class="w-full mb-16 px-4">
      <h1 class="font-heading font-bold text-6xl text-center">{{ page.title }}</h1>
    </div>
    <div class="w-full mb-36 px-4">
      <div class="basic-text">{{ page.content }}</div>
    </div>
  </div>
</section>
